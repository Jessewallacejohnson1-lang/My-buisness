{
  "name": "hygge-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web": "npm run dev --workspace @hygge/web",
    "web:build": "npm run build --workspace @hygge/web",
    "mobile": "npm run start --workspace @hygge/mobile"
  }
}
